# Hugging Face Space requirements.
#
# The Space is built from this subfolder, so the parent project is not
# available locally. Install it directly from the fork's main branch.
free-search-mcp @ git+https://github.com/artfix/web-search-mcp.git@main
uvicorn>=0.30
